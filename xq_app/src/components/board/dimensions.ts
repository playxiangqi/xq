export const [FILE_MAX, Y_MAX] = [0, 0];

// function clampPoint(file: number, rank: number): [number, number] => {
//   return [
//     Math.min(FILE_MAX, Math.max(0, Math.round(file))),
//     Math.min(RANK_MAX, Math.max(0, Math.round(rank))),
//   ]
// }

// export function pointToCoords(file: number, rank: number): [number, number] => {
//   const [clampedFile, clampedRank] = clampPoint(file, rank);
//   return [

//   ]
// }
